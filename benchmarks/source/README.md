# Private authoring source

Answer-bearing mutable source records live below this directory locally and are ignored by Git.
The sole supported M2.1 form is `source_manifest.json` plus `items.jsonl`. Do not place real
benchmark items in the repository during M2.1.
