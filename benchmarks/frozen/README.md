# Frozen benchmark versions

This directory contains no production benchmark in M2.1. Later write-once versions require exact
candidate, approval, Git, specification, rights, human-validation, ethics, and prerequisite hashes.
Filesystem read-only flags are advisory; manifests and application-level refusal are authoritative.
